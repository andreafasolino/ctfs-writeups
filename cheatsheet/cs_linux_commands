FTP:
    ftp 10.10.10.3

    //access as anonymous:
    ftp 10.10.10.5
    Connected to 10.10.10.5.
    220 Microsoft FTP Service
    Name (10.10.10.5:daerves): anonymous
    331 Anonymous access allowed, send identity (e-mail name) as password.
    Password:

    ftp> put prova.txt                  //upload


CRONTAB:
    crontab -l          (for current user)
    cat /etc/crontab



MYSQL:
  mysql -u username -p
  show databases;
  use dbname;
